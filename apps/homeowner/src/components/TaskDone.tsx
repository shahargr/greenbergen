// TaskDone moved to apps/shared/src/TaskSheet.tsx so every door opens the
// same update sheet. This file keeps the old import path alive; nothing in
// it is homeowner-specific any more.
export { TaskSheet as TaskDone } from "@shared/TaskSheet";
