# Copy to .env.local. Real keys are never committed.
# Publishable by design (RLS is the boundary); the service-role key never lives here.
NEXT_PUBLIC_SUPABASE_URL=https://oznqiwldgjrykadqsriv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Where the app is served - used in share links and emails.
NEXT_PUBLIC_APP_URL=http://localhost:3001
