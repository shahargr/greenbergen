// Which hats have a surface today, and where each one lives. Shared by the
// mask menu (server action) and the top nav label.
export const VIEW_HOME: Record<string, string> = {
  Owner: "/my",
  Contractor: "/contractor",
  PM: "/contractor",
  GC: "/contractor",
  Admin: "/admin",
};
